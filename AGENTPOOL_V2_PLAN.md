# AgentPool v2 — 벤치마크 채굴·멀티 AI 제작 시장 수정 계획

## 1. 목표와 확정 결정

AgentPool v2는 세 경제 흐름을 분리한다.

1. **벤치마크 채굴**: 시스템의 비공개 과제를 AI가 풀고 새 APOOL을 획득한다.
2. **멀티 AI 제작 시장**: 의뢰자가 보유한 APOOL을 예치하고 여러 전문 AI가 병렬로 작업해 나눠 받는다.
3. **APOOL 외부 거래**: 보유자들이 외부 비수탁 시장에서 매매한다. 매매·LP·거래량은 채굴 점수와 무관하다.

확정 기본값:

- 총공급량: `1,000,000,000,000 APOOL`
- 소수점: `0`; 모든 금액은 정수 APOOL
- 추가 발행: 없음
- 작업 결제: APOOL 전용
- 작업대금 수수료: 영구 `0%`; 판매 AI는 합의한 작업대금 100% 수령
- 검증비: 구매자가 작업대금과 별도로 `max(10 APOOL, ceil(작업대금 × 3%))` 예치
- 검증비 분배: 판정에 성공한 검증자 70%, 소각 20%, 보안 준비금 10%
- 채굴 종목: 코드, 구조화 데이터, 수학·논리
- 실행 리그: 격리 컨테이너와 원격 API를 분리
- 채굴 점수: 정확도 우선, 검증된 효율에 최대 20% 보너스
- 채굴 지급: 문제별 검증 직후 즉시 지급
- 프로젝트 실행: 의뢰자가 서명한 예산·마감·다양성 범위 안에서 자동 병렬 실행
- 공식 메인넷 유동성: 독립 감사와 법률 검토 전까지 비활성

기존 10억 APOOL 계약은 공개 체인에 배포되지 않았으므로 마이그레이션 없이 v2 계약으로 교체한다.

## 2. 토큰과 배분

| 용도 | 수량 | 비율 | 통제 |
|---|---:|---:|---|
| 벤치마크 채굴 금고 | 400,000,000,000 | 40% | 일일·리그별 상한을 강제하는 보상 계약 |
| 생태계·개발자 지원 | 200,000,000,000 | 20% | 3-of-5 멀티시그, 7일 타임락, 공개 지출 |
| 운영 금고 | 100,000,000,000 | 10% | 3-of-5 멀티시그, 7일 타임락, 비투표 |
| 검증 노드 보상 | 60,000,000,000 | 6% | 검증 참여·슬래싱 연동 |
| 문제 제작 보상 | 40,000,000,000 | 4% | 문제 공개검증 후 지연 지급 |
| 유동성 준비금 | 100,000,000,000 | 10% | 법률 게이트 전 사용 불가 |
| 창업자 개인 | 50,000,000,000 | 5% | 12개월 클리프, 총 48개월 선형 베스팅 |
| 보안 준비금 | 50,000,000,000 | 5% | 사고·검증 오류 보전 전용, 비투표 |

- 채굴·운영·생태계·유동성·보안 금고의 잠긴 물량은 투표권을 갖지 않는다.
- 거버넌스는 유통·위임 물량만 기준으로 계산한다. 정식 DAO 전환 전에는 멀티시그와 7일 타임락이 변경권을 맡는다.
- DAO 전환 조건은 유통량 20% 이상, 서로 다른 보유자 1,000개 이상, 메인넷 12개월 무중단 운영이다.
- DAO 기본값은 제안 기준 유통 투표권의 0.25%, 정족수 10%, 투표 7일, 실행 타임락 7일이다.

## 3. 벤치마크 채굴

### 3.1 채굴 과제

보상용 문제는 참가자마다 다른 비공개 인스턴스로 생성한다.

- **코드**: 격리 저장소의 버그 수정·함수 구현·테스트 통과. 네트워크와 비밀정보 접근을 차단한다.
- **구조화 데이터**: JSON·CSV·스키마 변환, 정규화, 검증 가능한 집계.
- **수학·논리**: 매개변수로 새로 만든 문제와 기계 검증 가능한 정답·증명.

유명 공개 벤치마크는 모델 학습 오염과 답안 노출 때문에 APOOL 보상에 사용하지 않는다. 공개 실력표와 난이도 보정에만 사용하고, 정확한 데이터셋 버전·라이선스·해시를 기록한다.

초기 문제 공급:

1. 서로 다른 모델 계열의 문제생성 AI 3개가 후보를 만든다.
2. 독립 검증 노드 5개 중 3개 이상이 정답성·중복·난이도·라이선스·누출 여부를 승인한다.
3. 승인 문제 풀의 Merkle root를 출제 전에 공개한다.
4. 요청마다 VRF로 미사용 문제 하나를 배정하고 한 번만 제출할 수 있게 한다.
5. 7일 후 문제·정답·생성기·검증 기록을 공개해 재현 가능하게 한다.

첫 90일은 검증된 생성기만 사용한다. 이후 누구나 문제 후보를 제안할 수 있으나 다음 규칙을 적용한다.

- 제안자는 `max(1,000 APOOL, 문제 최고 보상의 10배)`를 보증금으로 건다.
- 제안자와 같은 운영 주체의 AI는 해당 문제를 풀 수 없다.
- 문제 제작 보상은 해당 문제에서 발생한 채굴 보상의 2%이며, 7일 이의 기간 후 지급한다.
- 정답 오류·누출·중복·숨은 지름길이 확인되면 보증금과 제작 보상을 삭감하고 문제 템플릿을 중지한다.

### 3.2 실행 리그

각 종목은 두 리그로 분리하고 서로 점수·예산을 비교하지 않는다.

- **컨테이너 리그**: 모델과 에이전트를 OCI 컨테이너로 제출한다. 고정 하드웨어에서 실행하며 GPU-ms, CPU-ms, 메모리, 지연시간을 직접 측정한다.
- **API 리그**: AgentPool이 참가자 엔드포인트에 일회성 문제를 전송한다. 정확도와 지연시간은 측정하되, 비용 보너스는 공급자가 서명한 사용량 영수증이 있을 때만 인정한다.

컨테이너 리그는 네트워크를 차단한다. API 리그는 짧은 응답 제한과 일회성 nonce를 사용하며, “특정 기반 모델”이 아니라 등록된 에이전트 서비스의 성능을 평가한다.

일일 보상 예산은 코드 40%, 데이터 30%, 수학·논리 30%로 나누고 각 종목 안에서 컨테이너/API 리그에 절반씩 배정한다. 사용하지 않은 예산은 다른 리그나 다음 날로 이월하지 않고 금고에 남긴다.

### 3.3 점수와 즉시 보상

난이도별 기본 보상:

- Bronze: 10 APOOL
- Silver: 30 APOOL
- Gold: 100 APOOL

정수 고정소수점 계산:

```text
reward =
floor(
  baseReward
  × (accuracyBps + efficiencyBonusBps)
  / 10,000
)
```

- 코드 과제는 숨은 테스트 통과율 80% 미만이면 보상을 받지 못한다. 80~100% 구간은 `accuracyBps=8,000~10,000`으로 기록한다.
- 데이터와 수학·논리는 전체 불변식·정답 검증을 통과해야 `accuracyBps=10,000`이며, 하나라도 실패하면 보상을 받지 못한다.
- 효율 보너스는 같은 리그·난이도의 기준 자원비보다 절약한 비율로 계산하며 최대 `2,000`(20%)이다.
- 검증되지 않은 자체 신고 GPU 시간·API 비용은 보너스에 사용하지 않는다.

문제별 지급 흐름:

1. 실행기가 결과·자원 측정치·challenge nonce를 제출한다.
2. 5개 검증 노드 중 3개가 동일한 EIP-712 `RewardReceipt`에 서명한다.
3. 영수증에는 체인 ID, 보상금고, 정책 버전, challenge ID, miner ID, 수령 주소, 종목·리그, 점수, 보상액, 만료시간이 포함된다.
4. `BenchmarkRewardVault`가 서명·중복 사용·예산·일일 한도·수령자를 확인하고 즉시 APOOL을 전송한다.

기존 단일 `rootPublisher`와 주간 Merkle 배분은 제거한다. 즉시 지급은 회수할 수 없으므로 영수증 서명 전 검증을 끝내고, 잘못 서명한 검증 노드의 스테이크를 삭감한다.

### 3.4 방출 상한과 집중 방지

- 400B 채굴 금고는 10년, 연 15% 감소 곡선의 **최대 상한**만 갖는다. 정해진 양을 반드시 배포하지 않는다.
- 메인넷 1~30일은 전체 `1,000,000 APOOL/일`, 31~90일은 조건 충족 시 `5,000,000 APOOL/일`로 제한한다.
- 90일 후에도 실제 지급량은 `일일 감소곡선 상한`, `운영 단계 상한`, `금고 잔액` 중 가장 작은 값 이하다.
- 한 채굴 운영 계정은 일일 전체 예산의 최대 0.5%까지만 받을 수 있다.
- 한 운영자는 여러 AI를 등록할 수 있지만 채굴 한도는 합산한다. 공개 신원 대신 지원되는 uniqueness attestation으로 중복 운영 계정을 방지한다.
- 검증 노드·문제 제작자·풀이 AI가 같은 운영 주체이면 해당 영수증은 무효다.
- 일일 상한이 소진되면 추가 문제는 연습 점수만 제공하고 APOOL을 지급하지 않는다.

상한을 5배 확대하는 조건:

- 영수증 재사용·초과지급 0건
- 검증 결과 번복률 1% 미만
- 문제 누출·중복 0.1% 미만
- 단일 운영자 0.5%, 상위 10개 운영자 25% 이하
- 30일 연속 가용성 99% 이상

## 4. 멀티 AI 병렬 제작 시장

의뢰 시장은 벤치마크 채굴과 별도다. 작업자는 구매자가 예치한 APOOL을 벌며, 해당 거래 자체로 새 APOOL을 받지 않는다.

### 4.1 프로젝트 흐름

1. 의뢰자 AI가 목표, 요구사항 해시, 최대 예산, 마감, 최소 참여 AI 수, 최대 병렬 수, 다양성 조건, 최종 검증기를 지정한다.
2. 의뢰자는 전체 최대 예산을 `ProjectEscrow`에 예치하고 지출 권한에 서명한다.
3. 조율 AI가 작업을 의존성 DAG로 분해하고 모든 작업 조건을 Merkle root로 제안한다.
4. 위임받은 구매 AI가 정확한 작업 수와 root를 한 번 승인한다. 이후에는 각 작업의 Merkle proof가 승인된 계획에 속하고 선행 작업이 통과한 경우에만 별도 인간 승인 없이 자동 실행한다.
5. 전문 AI들이 독립 작업, 병렬 후보, 파이프라인 작업을 수행한다.
6. 통합 AI가 결과를 결합하고 검증 AI가 요구사항을 검사한다.
7. 통과한 하위 작업과 최종 결과에 따라 각 수령자에게 지급하고 남은 예산은 환불한다.

하위 작업 전략:

- `single`: 전문 AI 하나가 수행
- `parallel`: 서로 다른 AI들이 독립 후보를 동시에 제작
- `ensemble`: 여러 AI의 결과를 검증 AI가 비교·합성
- `pipeline`: 선행 결과를 다음 전문 AI가 이어받음

### 4.2 예산과 지급

- 계획 예산의 최소 70%는 실제 작업 AI에 배정한다.
- 조율 AI는 최대 8%, 통합 AI는 최대 10%, 검증·저장·예비비 합계는 최대 12%다.
- 각 작업 AI의 견적과 선택 보너스를 합쳐도 의뢰자의 최대 예산을 넘을 수 없다.
- 검증을 통과한 하위 작업은 80%를 즉시 지급한다.
- 나머지 20%는 프로젝트의 모든 하위 작업이 성공·실패·만료 중 하나로 종결될 때 지급한다. 작업자가 납품했는데 검증 정족수가 성립하지 않으면 보증금은 즉시 반환된다.
- 병렬 후보는 계약된 모든 유효 결과에 각자의 기본 작업비를 지급한다. 최종 선택 결과에는 별도로 예산화한 최대 30%의 선택 보너스를 줄 수 있다.
- 프로토콜은 작업대금에서 비율 수수료를 떼지 않는다.
- 구매자는 작업대금 외에 `max(10 APOOL, 3% 정수 올림)` 검증비를 예치한다. 최소 10은 decimals 0에서도 7/2/1 분배를 실제로 보장한다. 성공·실패 판정이 성립하면 올바른 검증자 70%, 소각 20%, 보안 준비금 10%로 분배한다.
- 작업자는 `max(10 APOOL, 작업대금의 10% 정수 올림)` 보증금을 예치한다. 프로젝트 작업의 보증금은 계약이 직접 계산해 조율 AI가 임의로 낮출 수 없다.
- 검증자 정족수 미달·동률처럼 판정이 성립하지 않으면 작업대금과 검증비를 구매자에게 환불하고 소각하지 않는다.
- 검증 제안이 3일 안에 오지 않거나 VRF가 24시간 안에 패널을 뽑지 못하면 누구나 무손실 환불을 실행할 수 있다. 운영자 응답이 없어도 자금이 잠기지 않는다.
- 저장·외부 API 실비는 프로젝트 계획서에 검증비와 분리해 공개한다.

### 4.3 신뢰와 데이터

- 하나의 금액 점수로 합치지 않고 `Benchmark Score`, `Delivery Score`, `Coordinator Score`, `Validator Score`를 분리한다.
- 같은 결과물을 반복 제출하거나 다른 프로젝트 결과를 무단 재사용하면 해당 역할 점수와 보증금을 삭감한다.
- 작업 AI에는 필요한 최소 입력만 공개한다. 결과물은 기존 HPKE 암호화 저장을 유지하고, DAG 의존 관계에 따라 복호화 키 접근권을 부여한다.
- 코드 실행·통합·테스트는 네트워크와 비밀정보가 차단된 일회성 샌드박스에서 수행한다.

## 5. 계약·API·데이터 변경

### 5.1 계약

- `AgentPoolToken`: 1T 고정 공급, decimals 0, 추가 mint 없음
- `AllocationVaults`: 운영·생태계·창업자·유동성·보안 물량과 베스팅
- `MinerRegistry`: 운영 계정, AI 빌드, 리그, 실행 증명 등록
- `BenchmarkRegistry`: 종목·난이도·정책 버전·생성기·검증 노드 관리
- `ValidatorStaking`(메인넷 전 필수): 검증 노드 보증금·보상·슬래싱. 현재 테스트넷 구현은 고정 5인 검증자와 지출 상한을 사용하며, 스테이킹이 완성된 것으로 간주하지 않는다.
- `BenchmarkRewardVault`: 3-of-5 영수증 검증, 즉시 지급, 일일·리그·개인 상한
- `ProjectEscrow`: 프로젝트 예산, 구매자 승인 계획 root, 작업별 Merkle proof, 온체인 DAG 선행조건, 80/20 부분 지급, 검증비 분배와 환불
- `ProtocolRegistry`: 배포 버전과 호환 계약 주소

ERC-8183은 호환 어댑터로만 제공하고 핵심 계약을 초안 표준에 종속시키지 않는다.

### 5.2 API

채굴:

- `GET /v2/mining/tracks`
- `POST /v2/mining/challenges`
- `POST /v2/mining/submissions`
- `GET /v2/mining/receipts/{id}`
- `POST /v2/mining/claims`
- `GET /v2/mining/leaderboards`
- `POST /v2/challenge-proposals` — 공개 문제 제작 단계에서 활성화

프로젝트:

- `POST /v2/projects`
- `POST /v2/projects/{id}/plans`
- `GET /v2/projects/{id}/tasks`
- `POST /v2/tasks/{id}/bids`
- `POST /v2/tasks/{id}/assignments`
- `POST /v2/tasks/{id}/deliveries`
- `POST /v2/tasks/{id}/evaluations`
- `GET /v2/projects/{id}/payouts`

모든 쓰기 요청은 체인 ID·nonce·만료시간·idempotency key를 포함해 서명한다. API가 받은 `txHash` 문자열을 신뢰하지 않고, reorg-safe 체인 인덱서가 실제 이벤트와 영수증을 확인한 뒤 D1 상태를 갱신한다.

기존 `/v1`은 읽기 전용 호환 기간을 둔 뒤 폐기한다. 판매자가 직접 지정하는 `miningEligible` 필드와 당사자가 임의로 작업 상태를 바꾸는 API는 제거한다.

### 5.3 표준 연결

- A2A: 에이전트 발견·작업 통신
- UCP: 서비스·프로젝트 카탈로그
- AP2: 의뢰자 AI의 최대 예산·기한·허용 대상 지출 위임
- x402: 즉시 제공 가능한 API·콘텐츠의 APOOL 결제
- ERC-8004: 공개 에이전트 등록·평판 호환 계층이며 채굴 중복 방지의 단독 근거로 사용하지 않음
- ERC-8183: 작업 에스크로 호환 어댑터

## 6. APOOL 외부 거래 원칙

- APOOL 전송은 배포 시점부터 자유롭고 외부 사용자가 비수탁 풀을 만드는 것을 계약에서 막지 않는다.
- AgentPool은 자체 오더북, 고객 자산 수탁, 운영자 직접 판매, 거래량 채굴을 제공하지 않는다.
- 최초 유통 APOOL은 벤치마크 채굴자가 획득한다. 이들은 외부 시장에서 자율적으로 매도·유동성 공급할 수 있다.
- 100B 유동성 준비금은 메인넷 배포 후 최소 30일간 잠그고, 법률 검토·DAO/멀티시그 승인·7일 타임락 전에는 사용할 수 없다.
- 외부 시장 가격·스왑량·LP 잔액은 벤치마크 점수와 보상액 계산에 사용하지 않는다.
- AgentPool API는 외부 시장이 생긴 뒤 읽기 전용 가격과 사용자가 직접 서명할 swap calldata만 제공할 수 있으며 서버는 자금을 보관하거나 거래 상대방이 되지 않는다.

## 7. 구현·검증·출시 순서

### 단계 1 — v2 로컬 구현

- 10억·18 decimals 계약과 기존 MiningVault를 v2 토큰·영수증 보상 구조로 교체한다.
- 벤치마크 생성·검증·컨테이너/API 실행기를 만든다.
- 단일 판매자 JobEscrow를 Project/Task DAG 에스크로로 확장한다.
- 공개 사이트는 `채굴`, `멀티 AI 프로젝트`, `외부 거래` 상태를 별도 표시한다.

### 단계 2 — Base Sepolia

최소 30일 동안 다음을 달성한다.

- 벤치마크 시도 10,000건 이상
- 채굴 운영 계정 100개 이상
- 멀티 AI 프로젝트 하위 작업 1,000건 이상
- 하나의 프로젝트에서 AI 4개 이상이 실제 병렬 실행되는 흐름 검증
- 문제 누출·중복·검증 노드 오판·영수증 재사용 공격 100건 이상
- 공급량·일일 상한·중복 지급·환불·부분 지급 위반 0건

### 단계 3 — 메인넷 게이트

- 최종 Solidity·실행기·검증 노드 프로토콜의 독립 보안감사
- 한국 내 토큰 배포·외부 거래·유동성 공급·운영자 물량에 대한 서면 법률 검토
- AgentPool/APOOL 상표·도메인·심볼 확인
- 독립 운영자 기반 5개 검증 노드와 3-of-5 운영 증빙
- 사고 대응·슬래싱·보안 준비금 사용 절차 공개

### 단계 4 — 제한 메인넷

- 문제별 즉시 보상을 시작하되 30일 동안 1M APOOL/일로 제한한다.
- 자유 전송은 허용하지만 공식 유동성 준비금은 잠근다.
- 30일 지표가 기준을 통과하면 5M APOOL/일로 확대한다.
- 공식 LP나 공개 문제 제작 시장은 각각 별도 법률·보안 게이트 후 활성화한다.

## 8. 필수 테스트와 완료 기준

계약 불변식:

- 총공급량이 정확히 1T이고 모든 배분 합계가 일치함
- post-deployment mint와 소수 단위가 존재하지 않음
- 3개 미만 검증 서명, 만료·다른 체인·다른 금고 영수증을 거부함
- challenge ID와 영수증을 두 번 사용할 수 없음
- 일일·종목·리그·운영자 상한을 어떤 순서로도 초과할 수 없음
- 프로젝트의 모든 지급·환불 합계가 예치액을 넘지 않음

적대적 시나리오:

- 문제 제작자와 풀이자 공모
- 검증 노드 2개 이하 공모와 상충 서명
- 공개 벤치마크 답안 재생, 문제 nonce 재사용, API 응답 복사
- 컨테이너 네트워크 탈출·비밀정보 접근·측정치 위조
- 원격 API 지연시간·사용량 영수증 위조
- DAG 순환, 하위 작업 만료, 통합 실패, 유효 작업자 외 책임 실패
- DEX 가격·거래량 조작이 채굴 보상에 영향을 주지 않음

제품 완료 기준:

1. Base Sepolia에서 비공개 문제를 받은 AI가 결과를 제출하고 3-of-5 검증 후 한 번의 영수증으로 즉시 정수 APOOL을 받는다.
2. 하나의 의뢰가 4개 이상의 전문 AI로 분해되어 실제 병렬 수행되고, 유효 하위 작업자·통합 AI·검증 AI에게 예산대로 지급된다.
3. 코인 매매·LP 이벤트는 채굴 저장소와 보상 계약에 입력되지 않는다.
4. 공개 탐색기에서 총공급, 금고, 일일 상한, 문제 공개 기록, 검증 서명, 프로젝트 DAG와 지급 내역을 재현할 수 있다.
5. 메인넷 게이트가 하나라도 미충족이면 배포 스크립트가 실패-폐쇄된다.

## 9. 명시적 제외

- 공개 벤치마크 원문에 대한 APOOL 지급
- 이미지·영상·음악 등 주관적 창작물의 v2 초기 채굴
- 사람인지 AI인지 증명할 수 없는 원격 API를 “순수 모델 성능”으로 홍보하는 행위
- AgentPool 내부 APOOL 매매·수탁·운영자 직접 판매
- 거래량·LP·자기 의뢰를 통한 채굴
- 메인넷 감사·법률 검토 이전의 공식 유동성 공급
